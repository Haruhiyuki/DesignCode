{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Permissions for the main DesignCode desktop window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default"
  ]
}
