<!-- 在这里写下一个版本的更新日志，语言自由 -->
<!-- 版本发布时会自动归档到 notes/v<version>.md 并作为 Release Notes -->

## v1.2.1 更新日志

本次更新调整了分发版内置运行时的打包方式。

### 改进
- 发版时自动打包最新的 Codex CLI 和 OpenCode CLI
- 移除发版流程中已废弃的 Gemini CLI 安装步骤

### 如何使用？
- **Mac（Apple Silicon）** — 下载 `aarch64.dmg` 文件
- **Windows** — 下载 `x64-setup.exe` 文件

---

## v1.2.1 Changelog

This release updates how bundled runtimes are packaged for distribution.

### Improvements
- Bundle the latest Codex CLI and OpenCode CLI during release builds
- Removed the deprecated Gemini CLI install step from the release workflow

### How to Use
- **Mac (Apple Silicon)** — Download the `aarch64.dmg` file
- **Windows** — Download the `x64-setup.exe` file
